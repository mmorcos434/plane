#include <iostream>
#include <string>
#include "exprtk.hpp"
#include "main.h"
using namespace std; 

int main() {

    cout << "Enter your equation" << endl;
    getline(cin,equation);

    // get the string how to parse it?
    // stack of sets
    // pop and ask the values from the user, 
    //if its equal to vary you can get the range plug and plot





}

